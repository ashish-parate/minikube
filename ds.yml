apiVersion: apps/v1
kind: DaemonSet

metadata:
  name: my-ds
  labels:
    app: my-app

spec:
  selector:
    matchLabels:
      app: my-app

  template:
    metadata:
      namespace: ops
      labels:
        app: my-app

    spec:
      containers:
        - name: my-app
          image: nginx:latest
          ports:
            - name: http
              containerPort: 80
              protocol: TCP